package uz.ithunter.crm.application;

/** Mirrors {@code ck_application_status} in V5 (FINAL_DOMAIN_MODEL.md 4.1). */
public enum ApplicationStatus {
    DRAFT, SUBMITTED, REGISTERED, RETURNED_TO_APPLICANT, CANCELLED
}
